---
profile_id: _template
markers: []          # このプロファイルを選ぶマーカーファイル（repo-profile.md §1-2 の表と一致させる）
---

# プロファイル: <言語 / フレームワーク>

新しいプロファイルを起こすときはこのファイルをコピーする。
**ここに書いてよいのは「その言語 / ライブラリを使うどのリポジトリでも成り立つこと」だけ。**
1 つのリポジトリでしか成り立たない事実は `<repo>/.claude/dual-review-profile.md` に書く。

判定に迷ったら: *別の会社の別のリポジトリでも同じことが言えるか?*
言えないならリポジトリ側へ。

## 規約ファイル候補

このエコシステムで規約が置かれがちなパス（存在確認は監督が毎回行う）。

- `<path>`

## テスト（既定値）

- ディレクトリ / 命名規則: `<pattern>`
- 実行コマンド: `<command>`
- レビュワーに実行させるか: 既定は **させない**（理由を書く）

## 生成物パターン

差分に混ざったら指摘対象になるもの（ビルド成果物・依存物・ローカル設定）。

- `<glob>`

## digest 対象（巨大な機械生成テキスト）

`SKILL.md` 1-5 の要約対象にする glob と、要約時に抜き出すキー。

- `<glob>` — 抜き出す情報: `<何>`

## 機械チェック（監督がフェーズ 1-4 で実行する）

言語固有で、**機械的に確実に判定できる**もの。コマンド付きで書く。

1. **<名前>**: <条件> → `WARNING: <メッセージ>`

## レビュー観点（`{{ECOSYSTEM_CHECKS}}` に流し込む本文）

以下がそのまま指示書に入る。レビュワーが読む文体で書く。

- <観点>

## このエコシステム由来の典型的な偽陽性

レビュワーが毎回引っかかる、言語仕様上は正しい書き方。

- <事象> — <なぜ問題ないか>
